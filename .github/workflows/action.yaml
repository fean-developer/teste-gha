name: Update README

on:
  push:
    branches:
      - main

permissions:
  contents: write 

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Add text to README
      run: echo "Texto adicionado pelo GitHub Actions" >> README.md
